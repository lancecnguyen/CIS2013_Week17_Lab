#include <iostream>
#include <fstream>

using namespace std;

#define DEBUG false

	
int add_numbers(int x, int y){
	return x+=y;
int main(){
	
	cout << "Adding 5 to 300: " << add_numbers(5,300);
	cout << "Adding 5 to 300: " << add_numbers(5,300);
	cout << "Adding 5 to 300: " << add_numbers(5,300);
	return 0;
}